import{useMemo,useState,type FormEvent}from"react";import{MetricCard,Modal}from"../components/AdminLayout";import{shopOrders as seed,type ShopOrder,type OrderStatus}from"../data/shop-orders.mock";import{shopProducts}from"../data/shop-products.mock";const money=(n:number)=>n.toLocaleString("vi-VN")+" đ",labels:Record<OrderStatus,string>={pending_payment:"Chờ xử lý",packing:"Đang đóng gói",shipping:"Đang giao 2H",completed:"Hoàn tất",cancelled:"Đã hủy",returned:"Hoàn trả"};
export default function ShopOrdersPage(){const[items,setItems]=useState(seed),[selected,setSelected]=useState<ShopOrder>(seed[0]),[tab,setTab]=useState<"all"|OrderStatus>("all"),[search,setSearch]=useState(""),[branch,setBranch]=useState("all"),[fulfill,setFulfill]=useState("all"),[modal,setModal]=useState<"pos"|"statement"|null>(null),[toast,setToast]=useState("");const notify=(x:string)=>{setToast(x);setTimeout(()=>setToast(""),1600)},shown=useMemo(()=>items.filter(o=>(tab==="all"||o.status===tab)&&(branch==="all"||o.branch===branch)&&(fulfill==="all"||o.fulfillmentType===fulfill)&&`${o.id} ${o.member.name} ${o.member.phone}`.toLowerCase().includes(search.toLowerCase())),[items,tab,branch,fulfill,search]);const update=(status:OrderStatus)=>{setItems(items.map(o=>o.id===selected.id?{...o,status}:o));setSelected({...selected,status});notify(`Đã cập nhật: ${labels[status]}`)};
const create=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const d=new FormData(e.currentTarget),p=shopProducts.find(x=>x.id===Number(d.get("product")))!,qty=Number(d.get("qty")),discount=Number(d.get("discount")),id=`QA-ORD-${Date.now().toString().slice(-5)}`,o:ShopOrder={id,createdAt:"Vừa tạo",member:{id:"WALK-IN",name:String(d.get("customer")),phone:String(d.get("phone")),email:"—",tier:"Khách POS",address:"Nhận tại quầy"},items:[{productId:p.id,sku:p.sku,name:p.name,qty,price:p.price}],total:p.price*qty-discount,paymentMethod:String(d.get("payment")),paymentStatus:"Đã thanh toán",branch:String(d.get("branch")),fulfillmentType:String(d.get("fulfillment")),status:"packing",shipper:"Chưa phân công",tracking:"POS-LOCAL",vat:Math.round(p.price*qty*.08)};setItems([o,...items]);setSelected(o);setModal(null);notify("Đã tạo đơn POS")};
return <div className="admin-page shop-page"><header className="page-heading"><div><p>KINH DOANH & THƯƠNG MẠI　›　QA PRO SHOP　›　<b>QUẢN LÝ ĐƠN HÀNG</b></p><h1>QUẢN LÝ ĐƠN HÀNG & ĐIỀU PHỐI VẬN CHUYỂN</h1></div><div className="heading-actions"><button onClick={()=>notify("Đã xuất HĐĐT / Excel mock")}>⇩ Xuất HĐĐT / Báo cáo Excel</button><button className="primary" onClick={()=>setModal("pos")}>＋ Tạo đơn hàng tại quầy POS</button></div></header><section className="order-metrics"><MetricCard label="TỔNG ĐƠN HÔM NAY" value="38" note="+14.2% doanh thu"/><MetricCard label="CHỜ KHỚP LỆNH" value="02" note="VietQR / Chuyển khoản"/><MetricCard label="ĐANG CHUẨN BỊ" value="08" note="Kho xuất"/><MetricCard label="GIAO HỎA TỐC 2H" value="12" note="Trên đường" tone="mint"/><MetricCard label="HOÀN TẤT HÔM NAY" value="16" note="100% On-time"/></section><section className="order-filters"><div className="chips">{(["all","pending_payment","packing","shipping","completed","cancelled"]as const).map(k=><button className={tab===k?"selected":""} onClick={()=>setTab(k)} key={k}>{k==="all"?"TẤT CẢ ĐƠN":labels[k]}</button>)}</div><div><label>⌕<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Mã đơn, tên hội viên, SĐT nhận..."/></label><select value={branch} onChange={e=>setBranch(e.target.value)}><option value="all">Điểm xuất kho: Tất cả</option>{[...new Set(items.map(x=>x.branch))].map(x=><option key={x}>{x}</option>)}</select><select value={fulfill} onChange={e=>setFulfill(e.target.value)}><option value="all">Phương thức nhận hàng</option>{[...new Set(items.map(x=>x.fulfillmentType))].map(x=><option key={x}>{x}</option>)}</select><button onClick={()=>notify("Bộ lọc nâng cao đã mở")}>☰</button></div></section><div className="orders-layout"><section><h2>Danh sách đơn Pro Shop</h2><div className="order-list">{shown.map(o=><article className={selected.id===o.id?"selected":""} onClick={()=>setSelected(o)} key={o.id}><header><b>#{o.id}</b><span className={`order-status ${o.status}`}>{labels[o.status]}</span><strong>{money(o.total)}</strong></header><p>{o.createdAt} · Kho: {o.branch}</p><div><span><b>{o.member.name}</b><small>{o.member.tier} · {o.member.phone}<br/>{o.member.address}</small></span><span>{o.items.map(x=><small key={x.sku}>{x.name} (x{x.qty})</small>)}</span></div><footer><small>{o.paymentStatus} · {o.paymentMethod}</small><button onClick={e=>{e.stopPropagation();notify("Đã tạo Bill & Vận đơn")}}>In Bill & Vận đơn</button><button onClick={e=>{e.stopPropagation();notify("Đã gọi shipper mock")}}>Gọi Shipper</button></footer></article>)}</div><div className="pagination"><span>Hiển thị {shown.length} đơn</span><div><button className="current">1</button><button>2</button><button>3</button></div></div></section><aside className="order-detail pt-panel"><header><div><h2>CHI TIẾT ĐƠN HÀNG</h2><b>#{selected.id}</b></div><small>Vận đơn: {selected.tracking}</small></header><section><h3>KHÁCH HÀNG / NGƯỜI NHẬN</h3><b>{selected.member.name}</b><p>{selected.member.id} · {selected.member.phone}<br/>{selected.member.email}<br/>{selected.member.address}</p></section><section><h3>SẢN PHẨM ĐÃ MUA</h3>{selected.items.map(x=><div className="detail-product" key={x.sku}><i>▣</i><span><b>{x.name}</b><small>{x.sku} · SL x{x.qty}</small></span><strong>{money(x.price*x.qty)}</strong></div>)}</section><section><h3>HÀNH TRÌNH VẬN CHUYỂN</h3>{["Đặt hàng","Khớp thanh toán","Xuất kho","Đóng gói","Shipper nhận hàng","Đang giao","Hoàn tất"].map((x,i)=><p className={i<({pending_payment:1,packing:4,shipping:6,completed:7,cancelled:1,returned:7}[selected.status])?"done":""} key={x}>● {x}</p>)}</section><section className="finance"><span>Tạm tính <b>{money(selected.total-selected.vat)}</b></span><span>Thuế GTGT <b>{money(selected.vat)}</b></span><strong>TỔNG THANH TOÁN {money(selected.total)}</strong></section><section><h3>SHIPPER & OTP GIAO HÀNG</h3><p>{selected.shipper} · OTP <b>8849</b></p><div className="signature">〰〰／＼〰</div></section><footer><button onClick={()=>notify("Đã xác nhận bàn giao")}>Xác nhận bàn giao</button><button onClick={()=>setModal("statement")}>Kiểm tra sao kê</button><button onClick={()=>{update("packing");notify("Đã khớp tay thủ công")}}>Khớp tay thủ công</button><button className="primary" onClick={()=>update("shipping")}>Cập nhật Đã Xuất Kho</button></footer></aside></div>
{modal==="pos"&&<Modal title="TẠO ĐƠN HÀNG TẠI QUẦY POS" onClose={()=>setModal(null)}><form onSubmit={create}><div className="form-grid"><label>Khách hàng<input name="customer" required/></label><label>SĐT<input name="phone" required/></label><label>Sản phẩm<select name="product">{shopProducts.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><label>Số lượng<input name="qty" type="number" min="1" defaultValue="1" required/></label><label>Giảm giá<input name="discount" type="number" defaultValue="0"/></label><label>Thanh toán<select name="payment"><option>Tiền mặt POS</option><option>VietQR</option></select></label><label>Nhận hàng<select name="fulfillment"><option>Nhận tại quầy</option><option>Giao Hỏa tốc 2H</option></select></label><label>Cơ sở<select name="branch"><option>Landmark Center HQ</option><option>Thảo Điền Hub</option></select></label></div><footer className="modal-actions"><button type="button" onClick={()=>setModal(null)}>Hủy</button><button className="primary">Tạo đơn POS</button></footer></form></Modal>}{modal==="statement"&&<Modal title="KIỂM TRA SAO KÊ" onClose={()=>setModal(null)}><p>Giao dịch mock: {selected.paymentMethod} · {money(selected.total)}</p><footer className="modal-actions"><button onClick={()=>setModal(null)}>Đóng</button><button className="primary" onClick={()=>{setModal(null);update("packing")}}>Khớp tay thủ công</button></footer></Modal>}{toast&&<div className="toast">✓ {toast}</div>}</div>}
